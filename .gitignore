node_modules/
dist/
dist-bin/
# Generated at publish time from the release binaries; ships in the npm tarball only.
packages/enigma-cli/bin/checksums.json
# Downloaded per-host at install time (binary + its sha256 sidecar); never committed.
packages/enigma-cli/bin/enigma-bin*
.dev-home/
*.log
.DS_Store
.env
.env.*
!.env.example
!.env.sample
!.env.template

# Internal-only content (never published)
/internal/
/docs/internal/

# Local reference clones (third-party repos kept only for study; never committed or published)
/references/

# Scratch dirs for temporary files (never committed)
/temp/
/tmp/

# Claude Code local runtime state (not project content)
/.claude/scheduled_tasks.lock

# Internal task ledger + one-off dev scratch (kept local; may reference upstream sources)
/.task/
/packages/linter/_fiximp2.mjs